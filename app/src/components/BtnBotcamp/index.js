/**
  * this kind of file become loading process in browser much lighter than if it was in the main index as done before
**/

import React, {Component} from "react";
import "./style.css"

const BtnBotcamp = () => (
  <button className="btn-botcamp">Enter</button>        // code is jsx that looks like an html code with some specificity like className instead just Name that can give some problem
);

export default BtnBotcamp;
